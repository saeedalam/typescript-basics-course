export const hello = "world"

// types


const addTwoNumbers = (a, b) => {
    return a + b
}

const result = addTwoNumbers(10, 20)
console.log(result)

// const resultAsString = result.toFixed()
// const secondResult = addTwoNumbers(1 , resultAsString)
// const secondResult = addTwoNumbers(1 , parseInt(resultAsString))